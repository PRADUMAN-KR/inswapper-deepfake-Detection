INSWAPPER_MODEL_PATH=checkpoints/best_model.pt
# Optional. If omitted, the API uses the threshold saved inside the checkpoint.
# INSWAPPER_THRESHOLD=0.5
INSWAPPER_DEVICE=auto
INSWAPPER_MAX_UPLOAD_MB=100
INSWAPPER_FACE_DETECTOR=insightface
INSWAPPER_FACE_MIN_CONFIDENCE=0.5
